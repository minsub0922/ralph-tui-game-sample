# TUI Dino Game

터미널에서 스페이스 한 키로 즐기는 공룡 점프 게임. 4시간 해커톤 결과물.

## Target user

사내 해커톤의 **심사자와 관람객**.

부스 앞에 30초 정도 머무르며, 발표자의 설명을 듣고 다음 부스로 이동한다. 노트북에 낯선 것을 설치할 생각이 없고, 브라우저를 열거나 계정을 만들 의사도 없다. 이미 터미널이 떠 있는 상태로 앉는다.

## Problem

심사자는 직접 손을 대보지 못한 채 설명만 듣고 지나간다. 조작해 본 경험이 없으면 "잘 만들었다"는 체감이 생기지 않고, 평가는 발표자의 말솜씨와 인상에만 의존하게 된다.

**손으로 조작하는 30초가 없으면 제품은 전달되지 않는다.** 그 30초 안에 설치·설명·학습이 끼어들 자리는 없다.

## Core action

**스페이스 한 키로 점프해 장애물을 피하며 한 판을 끝까지 플레이한다.**

시작 시 난이도 3단계 중 하나를 고르는 단계가 앞에 붙지만, 게임이 시작된 뒤 조작은 스페이스 하나로 고정된다. 배울 것이 없어야 30초 안에 체험이 끝난다.

## Observable outcome

성공 여부는 사람의 눈이 아니라 **헤드리스 시뮬레이션**이 판정한다.

- `npm run sim` 이 난이도 3종(slow / normal / fast) 각각에 대해 고정 시드로 한 판을 끝까지 돌린다.
- 각 판이 크래시 없이 목표 틱까지 생존하고, `난이도 / 시드 / 최종 점수 / 진행 틱 수 / 종료 사유`를 stdout에 출력한다.
- 세 판 모두 성공하면 exit code 0, 하나라도 도중에 죽거나 예외가 나면 exit code 1.

화면에서도 확인 가능하다. 게임 중에는 상단에 현재 점수와 최고 점수가 표시되고, 게임 오버 시 최종 점수·최고 기록·신기록 여부가 패널로 뜬다. 최고 기록은 난이도별로 로컬 JSON 파일에 저장되어 프로그램을 껐다 켜도 남는다.

## Happy path

1. 심사자가 `npm start` 를 실행한다.
2. 난이도 선택 화면이 뜬다. `1 / 2 / 3` 또는 위아래 방향키로 slow / normal / fast 중 하나를 고르고 Enter.
3. 지면 위에 공룡이 달리기 시작한다. 오른쪽에서 선인장이 다가온다.
4. 스페이스를 눌러 점프한다. 공룡이 떠올랐다가 중력으로 내려온다. 체공 중 추가 입력은 무시된다.
5. 생존하는 동안 점수가 올라간다. 상단에 현재 점수와 해당 난이도의 최고 점수가 함께 보인다.
6. 선인장에 부딪히면 게임 오버 패널이 뜬다. 최종 점수, 최고 기록, 신기록 달성 여부, `R = 다시하기 / Q = 종료` 안내가 함께 나온다.
7. `R` 을 누르면 같은 난이도로 즉시 재시작한다. `Q` 를 누르면 터미널이 원래 상태로 복구되고 종료된다.

## Success criteria

1. `npm run check` 가 통과한다 (타입 체크 + 전체 테스트).
2. `npm run sim` 이 난이도 3종 모두를 완주하고 exit code 0 으로 끝난다.
3. 같은 시드 + 같은 입력 시퀀스는 항상 같은 최종 점수를 낸다 (테스트로 검증).
4. 게임 로직은 터미널 없이 실행된다. `src/core/` 는 `process.stdout` / `process.stdin` / `node:fs` 를 참조하지 않는다.
5. `npm start` 후 스페이스만으로 게임을 진행할 수 있고, 게임 오버 → `R` 재시작 → `Q` 종료가 동작한다.
6. `Q` 또는 `Ctrl+C` 종료 시 터미널 raw mode 와 커서가 원래대로 복구된다.
7. 최고 기록이 난이도별로 파일에 저장되고, 재실행 시 다시 읽힌다.
8. 런타임 의존성 0개. Node.js 내장 모듈만 사용한다.

## Non-goals

이번 4시간 행사에서 **명시적으로 만들지 않는 것**:

- 인증, 로그인, 사용자 계정
- 결제
- 배포, 패키징, npm publish, Docker
- 외부 API 호출, 네트워크 통신 일체 — 완전 오프라인으로 동작한다
- 온라인 리더보드, 서버 동기화
- 사운드, 색상 테마 설정, 스프라이트 커스터마이징
- 웅크리기(duck), 새(pterodactyl) 등 추가 장애물 타입 — 조작은 스페이스 1키로 고정
- 세이브 / 이어하기 / 일시정지
- 터미널 리사이즈 실시간 대응 — 시작 시 크기를 1회만 확인하고, 최소 크기(60x16) 미만이면 안내 후 종료한다
- 마우스 입력
- Windows 터미널 대응 (macOS / Linux 터미널 기준)
